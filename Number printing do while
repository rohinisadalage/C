#include <stdio.h>
int main() {
    int number = 1;  
    int limit;      
    printf("Enter the limit: ");
    scanf("%d", &limit);
    do {
        printf("%d\n", number);
        number++;
    } while (number <= limit);
    return 0;
}
