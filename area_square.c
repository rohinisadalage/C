#include<stdio.h>
int main()
{
	int area,l,b;
	printf("Enter the value of l:");
	scanf("%d",&l);
	printf("Enter the value of b:");
	scanf("%d",&b);
	area=l*b;
	printf("area of square is%d",area);
	return 0;
}
