#include <stdio.h>
int main() {
    int n, i = 1; 
    printf("Enter the number up to which you want to print: ");
    scanf("%d", &n);
    while (i <= n) {
        printf("%d\n", i);
        i++; /
    }
    return 0;
}
